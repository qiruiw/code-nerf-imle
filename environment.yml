name: codenerf
channels:
  - pytorch
  - defaults
dependencies:
  - python>=3.8
  - pip
  - pip:
      - opencv-python
      - tensorboard
      - imageio
      - imageio-ffmpeg
      - lpips
  - scipy
  - numpy
  - matplotlib
  - scikit-image
  - tqdm